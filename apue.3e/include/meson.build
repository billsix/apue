# `apue_inc` is declared at the top level (as include_directories('include'))
# and consumed by lib/meson.build via libapue_dep. This file exists so
# meson's subdir() call has a target; nothing else to do here.
